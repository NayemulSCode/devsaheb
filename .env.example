# Generate both with:  node scripts/hash-password.mjs "your password"
# Never commit the real .env - it is gitignored.
ADMIN_PASSWORD_HASH=
SESSION_SECRET=

# Set to 1 in production so session cookies carry the Secure flag.
# Leave unset for local http development or the cookie will not be stored.
SECURE_COOKIES=

PORT=3000
